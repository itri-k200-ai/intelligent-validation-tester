# 除錯截圖

跟 Claude 討論前端畫面時丟的截圖：確認是否符合預期、或指出哪裡不對。

**這個目錄的圖片不進版控**（見同層 `.gitignore`）—— 屬於一次性的溝通素材，
留在版控裡只會讓 repo 愈來愈肥。要保留的畫面請放到 `docs/外部文件/` 或
`docs/design/`。

## 命名建議

`YYYY-MM-DD_哪一面牆_問題.png`，例如
`2026-08-27_右牆_三格文字過小.png`。

牆的部分用 `左牆` / `中牆` / `右牆` / `全牆`，方便一眼看出是哪個畫面。
