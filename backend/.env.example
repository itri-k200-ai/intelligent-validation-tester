DEBUG=True
SECRET_KEY=change-me-to-a-long-random-string
ALLOWED_HOSTS=localhost,127.0.0.1,backend-web

DATABASE_URL=postgres://ivt:ivtpass@postgres:5432/ivt

REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/1
CELERY_RESULT_BACKEND=redis://redis:6379/2
CHANNEL_REDIS_URL=redis://redis:6379/3

CORS_ORIGINS=http://localhost:3000,http://localhost

MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=reports

MEDIA_SERVER_API=http://mediamtx:9997

USE_MOCK_CONNECTORS=true
O1_USERNAME=
O1_PASSWORD=

# ── 場域測試(智慧網路中牆)──────────────────────────────────────
# 外部 Performance_tester(只有後端這台連得到場域網段)
PERF_TESTER_BASE=http://10.194.87.115:8011/api
PERF_TESTER_API_KEY=
# 情境 → 控制器與方案(JSON 字串;cid 來自 GET /api/ctrl-conns)
FIELD_TEST_TARGETS={"indoor":{"cid":"","ref":"amr-01","plan_id":""},"outdoor":{"cid":"","ref":"uav-01","plan_id":""}}
# IM adapter —— 共通性測試平台通知「顯示某筆歷史」後,adapter 只把它標成
# notified,所以牆面輪詢時順便問它的 /autoTest/history。留空 = 不問。
FIELD_TEST_ADAPTER_BASE=http://10.194.87.115:8012
# 設了就要求打 POST /api/field-tests/history/ 的人帶 X-Notify-Token
FIELD_TEST_NOTIFY_TOKEN=
